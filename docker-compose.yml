services:
  agent:
    build:
      context: .
      target: release
    ports:
      - "7860:7860"
    env_file:
      - .env
    environment:
      - GRADIO_SERVER_PORT=7860
      - UV_LINK_MODE=copy
    volumes:
      - .:/app
      - venv:/app/.venv

volumes:
  venv: